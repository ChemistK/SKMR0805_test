# 이 문서는 이 프로젝트를 작업할 때 반드시 지켜야 할 규칙입니다.
# 코드를 작성하거나 수정하기 전에 항상 이 문서를 먼저 확인하세요.

## 코드 작성 규칙

### 1. 기술 스택 제한
- 이 프로젝트는 프레임워크(React, Next.js 등)를 사용하지 않고 오직 **순수 HTML, CSS, JavaScript**로만 구성합니다.

### 2. 비개발자를 위한 주석
- 이 프로젝트의 주인은 개발 경험이 없는 사람입니다.
- 이해하기 어려운 코드나 복잡한 로직에는 반드시 쉬운 말로 주석을 달아주세요.
- **"이 부분은 무엇을 하는지, 왜 필요한지"**를 일상적인 표현으로 설명해주세요.
- 전문 용어를 써야 한다면, 괄호로 간단한 뜻풀이를 함께 적어주세요.

### 3. 역할 분리 (책임 분리)
- 한 파일이나 한 함수가 너무 많은 일을 하지 않도록 기능별로 나눠주세요.
- HTML은 **화면 구조**, CSS는 **디자인**, JavaScript는 **기능 구현**으로 명확히 분리하여 관리합니다.
- 나중에 한 부분을 고칠 때 다른 부분에 영향을 주지 않도록 깔끔하게 분리해주세요.

### 4. 하드코딩 최소화
- 나중에 바뀔 수 있는 값 (예: 안내 문구, 버튼 이름, 설정값 등)을 코드 여기저기에 직접 박아두지 마세요.
- 이런 값들은 파일 상단이나 별도 공간에 모아두고 가져다 쓰는 방식으로 만들어서, 수정할 때 한 군데만 고치면 되도록 해주세요.

### 5. 오류 처리 (에러 핸들링)
- 문제가 생겼을 때 화면이 그냥 멈추거나 동작하지 않는 일이 없도록, 오류 상황을 항상 대비해주세요.
- 오류가 나면 **"무엇이 왜 잘못됐는지"** 원인을 명확히 알 수 있게 메시지를 남겨주세요.
- 사용자에게는 이해하기 쉬운 안내 메시지를 보여주고 (예: **"검토 요청에 실패했어요. 잠시 후 다시 시도해주세요."**), 개발자용 상세 오류는 브라우저 콘솔창에 따로 기록되게 해주세요.

### 6. 보안 키 및 환경 변수 관리
- API 키처럼 외부에 노출되면 안 되는 중요한 값은 코드 안에 직접 적지 마세요.
- 반드시 프로젝트 최상위에 `config.js` 파일을 생성하고, 이 파일 안에 `CONFIG` 객체 형태로 중요한 값들을 저장하여 관리하세요.
- `index.html` 파일에서 다른 스크립트보다 **`config.js`를 먼저 불러오도록** 설계하여 코드에서 값을 안전하게 꺼내 쓸 수 있도록 해주세요.
- `config.js` 파일이 깃허브 등에 실수로 공유되지 않도록, 소스코드를 제공할 때 `.gitignore` 설정 안내를 항상 함께 포함해주세요.

## 작업 태도

### 개발 팀장으로서의 검토
- 스스로를 이 프로젝트를 책임지는 **"개발 팀장"**이라고 생각하고 작업하세요.
- 코드를 작성한 뒤에는 오류가 없는지 엄격하게 스스로 검토하세요.
- 문제를 발견하면 직접 수정하고, 실제로 실행해서 정상 작동하는지 확인한 뒤 완료로 처리하세요.
- **"일단 됐겠지"** 하고 넘어가지 말고, 확실하게 동작하는 것까지 책임지세요.